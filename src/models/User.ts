export interface User {
  email: string;
  name: string;
  subscription: string;
}

export interface IUser {
  name: string;
  email: string;
  customerId: string;
  priceId: string;
  hasAccess: boolean;
}
